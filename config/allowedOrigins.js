const allowedOrigins = [  'http://localhost:3000' 
// Code Review - Replace with the actual origin which should be maintained in .env file and use process.env.CORS_ORIGIN
];

module.exports = allowedOrigins;